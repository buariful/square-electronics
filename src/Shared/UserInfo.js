import React from 'react';

const UserInfo = ({ user, gUser }) => {
    console.log("user", user, 'guser', gUser)
    return (
        <div>
            jljljl
        </div>
    );
};

export default UserInfo;